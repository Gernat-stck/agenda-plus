---
import EmptyState from "../components/EmptyState.astro";
import Header from "../components/Header.astro";
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <div class="max-w-7xl mx-auto p-6">
    <!-- Header -->
    <Header
      title="Citas"
      subtitle="Agenda+"
      userName="Usuario"
      userRole="Emprendedor"
    />

    <!-- Main Content -->
    <main>
      <div class="flex justify-between items-center mb-8">
        <h2 class="text-xl font-semibold text-gray-800">Citas Agendadas</h2>
        <div class="flex gap-4">
          <button
            class="flex items-center gap-2 bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-600 transition-colors"
          >
            <i class="ri-calendar-2-line"></i>
            Calendario
          </button>
          <button
            class="flex items-center gap-2 bg-purple-600 text-white px-4 py-2 rounded-lg hover:bg-purple-700 transition-colors"
          >
            Crear Cita
            <i class="ri-add-line"></i>
          </button>
        </div>
      </div>

      <!-- Empty State -->
      <EmptyState />
    </main>
  </div>
</Layout>
